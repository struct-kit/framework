package main

import (
	"fmt"
	"os"

	"__MODULE_PLACEHOLDER__/internal/app/cli"
)

func main() {
	root := cli.NewRootCommand()
	if err := root.Execute(os.Args[1:]); err != nil {
		fmt.Fprintln(os.Stderr, err)
		os.Exit(1)
	}
}
